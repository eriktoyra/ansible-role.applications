---

- name: Installs Homebrew cask applications
  homebrew_cask:
    name: "{{ item }}"
    state: present
  with_items:
    - boostnote
    - caffeine
    - google-chrome
    - insomnia
    - slack
    - spotify
    - visual-studio-code
  tags: applications